/*
import React,{useState} from "react";

const serviceRegister = () =>{
}

const SendUseData = () =>{
    const [loading,setLoading] = useState('');
    const [response,setResponse] = useState(false);
    const [error,setError] = useState(false);

    const create_usuario = (data: any) => {
        setLoading(true)
        response =  serviceRegister(data)
        setLoading(false)
        return {loading,create_usuario,response,error}
    }
}

export default SendUseData;
*/